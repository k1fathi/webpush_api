# Basic model initialization with lazy imports to avoid circular dependencies
# Don't import everything at once to prevent errors during startup

# Empty init file is better for now - individual modules can import what they need
